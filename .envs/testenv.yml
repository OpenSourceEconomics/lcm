---
name: lcm
channels:
  - conda-forge
  - nodefaults
dependencies:
  - pip
  - setuptools_scm
  - toml

  # Testing dependencies
  - scipy
  - pybaum

  # Testing infrastructure
  - pytest
  - pytest-cov
  - pytest-xdist

  # Install lcm and its dependencies locally
  - pip:
      - -e ../
