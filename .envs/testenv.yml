---
name: lcm
channels:
  - conda-forge
  - nodefaults
dependencies:
  - pip
  - setuptools_scm
  - toml

  # Package dependencies
  - dags
  - jax>=0.4.10
  - jaxlib>=0.4.10
  - numpy
  - pandas

  # Testing dependencies
  - scipy
  - pybaum

  # Testing infrastructure
  - pytest
  - pytest-cov
  - pytest-xdist

  # Install lcm locally
  - pip:
      - -e ../
